INIT:
	init.sh
BUILD:
	chaneg DEBFULLNAME & DEBEMAIL for you
	$vi export.sh
	$. export.sh
	$cd skia
	$vi BUILD.gn
	Append "dl" in the libs in the component("skia") for dynamic build as below
	libs=["dl"]
	$sudo mk-builddeps -r -i
	$dpkg-buildpackage -b -uc -us
